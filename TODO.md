  - Add state for uploadType to track which upload option is active
- Modify handleFileUpload to set uploadType to 'file' and disable folder input
- Modify handleDirectoryUpload to set uploadType to 'folder' and disable file input
- Update UI to conditionally disable inputs based on uploadType
- Improve visual styling of the options area in App.css
